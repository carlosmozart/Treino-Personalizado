{
  "name": "treino-personalizado",
  "version": "2.20.1",
  "private": true,
  "description": "Aplicativo local de treino, preparado para PWA e Android com Capacitor.",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "check": "node scripts/check-web.mjs",
    "test": "node tests/release-readiness.mjs",
    "test:unit": "vitest run",
    "test:smoke": "playwright test",
    "verify": "npm run check && npm run test && npm run test:unit",
    "sync:android": "npm run prepare:web && npx cap sync android",
    "open:android": "npm run sync:android && npx cap open android",
    "build:apk": "npm run sync:android && cd android && gradlew.bat assembleRelease",
    "build:aab": "npm run sync:android && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/local-notifications": "^8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "@playwright/test": "^1.63.0",
    "vitest": "^5.0.1"
  }
}
